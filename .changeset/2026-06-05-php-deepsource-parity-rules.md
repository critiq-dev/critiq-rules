---
"@critiq/rules": minor
---

Add 26 PHP catalog rules for DeepSource analyzer parity: one security rule (`unsafe-new-static`), 23 correctness rules, and one performance rule (`expensive-loop-condition`).
