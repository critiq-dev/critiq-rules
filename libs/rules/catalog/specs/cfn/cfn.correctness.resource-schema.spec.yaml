apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/cfn/cfn.correctness.resource-schema.rule.yaml
fixtures:
  - name: flags E3000 finding
    observationPath: ./fixtures/cfn.correctness.resource-schema/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - cfn.correctness.resource-schema
      allSeverities:
        - high
  - name: ignores unrelated findings
    observationPath: ./fixtures/cfn.correctness.resource-schema/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
