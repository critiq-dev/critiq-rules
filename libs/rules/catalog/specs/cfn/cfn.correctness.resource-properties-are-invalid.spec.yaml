apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/cfn/cfn.correctness.resource-properties-are-invalid.rule.yaml
fixtures:
  - name: flags E3002 finding
    observationPath: ./fixtures/cfn.correctness.resource-properties-are-invalid/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - cfn.correctness.resource-properties-are-invalid
      allSeverities:
        - high
  - name: ignores unrelated findings
    observationPath: ./fixtures/cfn.correctness.resource-properties-are-invalid/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
