apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/cfn/cfn.correctness.template-size-limit.rule.yaml
fixtures:
  - name: flags E1002 finding
    observationPath: ./fixtures/cfn.correctness.template-size-limit/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - cfn.correctness.template-size-limit
      allSeverities:
        - high
  - name: ignores unrelated findings
    observationPath: ./fixtures/cfn.correctness.template-size-limit/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
