apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/cfn/cfn.correctness.validates-foreach-functions.rule.yaml
fixtures:
  - name: flags E1032 finding
    observationPath: ./fixtures/cfn.correctness.validates-foreach-functions/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - cfn.correctness.validates-foreach-functions
      allSeverities:
        - high
  - name: ignores unrelated findings
    observationPath: ./fixtures/cfn.correctness.validates-foreach-functions/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
