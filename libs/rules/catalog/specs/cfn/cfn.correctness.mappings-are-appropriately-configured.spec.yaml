apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/cfn/cfn.correctness.mappings-are-appropriately-configured.rule.yaml
fixtures:
  - name: flags E7001 finding
    observationPath: ./fixtures/cfn.correctness.mappings-are-appropriately-configured/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - cfn.correctness.mappings-are-appropriately-configured
      allSeverities:
        - high
  - name: ignores unrelated findings
    observationPath: ./fixtures/cfn.correctness.mappings-are-appropriately-configured/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
