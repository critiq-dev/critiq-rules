apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/java/java.correctness.invalidated-iterator.rule.yaml
fixtures:
  - name: flags collection modification in for-each loop
    observationPath: ./fixtures/java.correctness.invalidated-iterator/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - java.correctness.invalidated-iterator
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
