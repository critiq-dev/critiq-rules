apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/java/java.correctness.hashtable-contains-value.rule.yaml
fixtures:
  - name: flags Hashtable.contains() call
    observationPath: ./fixtures/java.correctness.hashtable-contains-value/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - java.correctness.hashtable-contains-value
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
