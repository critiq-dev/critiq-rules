apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/java/java.correctness.stream-reuse.rule.yaml
fixtures:
  - name: flags stream consumed twice
    sourcePath: ./fixtures/java.correctness.stream-reuse/invalid.java
    expect:
      findingCount: 1
      allRuleIds:
        - java.correctness.stream-reuse
  - name: allows each stream consumed once
    sourcePath: ./fixtures/java.correctness.stream-reuse/valid.java
    expect:
      findingCount: 0
      allRuleIds: []
