apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/java/java.correctness.possible-null-access.rule.yaml
fixtures:
  - name: flags chained .get() call without null check
    observationPath: ./fixtures/java.correctness.possible-null-access/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - java.correctness.possible-null-access
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
