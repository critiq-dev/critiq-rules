apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/java/java.correctness.timezone-invalid-id.rule.yaml
fixtures:
  - name: flags TimeZone.getTimeZone() with hardcoded string
    observationPath: ./fixtures/java.correctness.timezone-invalid-id/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - java.correctness.timezone-invalid-id
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
