apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/java/java.correctness.mutable-data-exposed.rule.yaml
fixtures:
  - name: flags mutable collection assigned without defensive copy
    observationPath: ./fixtures/java.correctness.mutable-data-exposed/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - java.correctness.mutable-data-exposed
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
