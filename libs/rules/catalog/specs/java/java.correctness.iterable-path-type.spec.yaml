apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/java/java.correctness.iterable-path-type.rule.yaml
fixtures:
  - name: flags Iterable<Path> type usage
    observationPath: ./fixtures/java.correctness.iterable-path-type/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - java.correctness.iterable-path-type
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
