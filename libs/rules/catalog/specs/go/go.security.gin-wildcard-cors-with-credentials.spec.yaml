apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/go/go.security.gin-wildcard-cors-with-credentials.rule.yaml
fixtures:
  - name: flags wildcard origins with credentials enabled
    sourcePath: ./fixtures/go.security.gin-wildcard-cors-with-credentials/invalid.go
    expect:
      findingCount: 1
      allRuleIds:
        - go.security.gin-wildcard-cors-with-credentials
  - name: accepts explicit origins with credentials
    sourcePath: ./fixtures/go.security.gin-wildcard-cors-with-credentials/valid.go
    expect:
      findingCount: 0
      allRuleIds: []
