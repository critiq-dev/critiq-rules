apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/go/go.correctness.time-tick-leak.rule.yaml
fixtures:
  - name: flags time.Tick usage
    sourcePath: ./fixtures/go.correctness.time-tick-leak/invalid.go
    expect:
      findingCount: 1
      allRuleIds:
        - go.correctness.time-tick-leak
  - name: accepts time.NewTicker with Stop
    sourcePath: ./fixtures/go.correctness.time-tick-leak/valid.go
    expect:
      findingCount: 0
      allRuleIds: []
