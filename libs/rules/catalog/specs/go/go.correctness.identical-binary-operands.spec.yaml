apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/go/go.correctness.identical-binary-operands.rule.yaml
fixtures:
  - name: flags identical binary operands
    sourcePath: ./fixtures/go.correctness.identical-binary-operands/invalid.go
    expect:
      findingCount: 1
      allRuleIds:
        - go.correctness.identical-binary-operands
  - name: accepts different binary operands
    sourcePath: ./fixtures/go.correctness.identical-binary-operands/valid.go
    expect:
      findingCount: 0
      allRuleIds: []
