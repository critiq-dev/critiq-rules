apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../rules/ts.security.exposed-directory-listing.rule.yaml
fixtures:
  - name: flags exposed directory listing middleware
    sourcePath: ./fixtures/public-security/render-express.invalid.ts
    expect:
      findingCount: 1
      allRuleIds:
        - ts.security.exposed-directory-listing
      allSeverities:
        - medium
  - name: ignores apps without directory listing middleware
    sourcePath: ./fixtures/public-security/render-express.valid.ts
    expect:
      findingCount: 0
      allRuleIds: []

