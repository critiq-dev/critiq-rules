apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/typescript/ts.security.permissive-file-permissions.rule.yaml
fixtures:
  - name: flags world-accessible file and directory modes
    sourcePath: ./fixtures/filesystem-safety/permissions.invalid.ts
    expect:
      findingCount: 2
      allRuleIds:
        - ts.security.permissive-file-permissions
        - ts.security.permissive-file-permissions
      allSeverities:
        - high
        - high
  - name: ignores restrictive file modes
    sourcePath: ./fixtures/filesystem-safety/permissions.valid.ts
    expect:
      findingCount: 0
      allRuleIds: []
