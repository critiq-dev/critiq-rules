apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/typescript/ts.security.predictable-token-generation.rule.yaml
fixtures:
  - name: flags predictable token generation
    observationPath: ./fixtures/ts.security.predictable-token-generation/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ts.security.predictable-token-generation
      allSeverities:
        - high
  - name: ignores crypto-backed token generation
    observationPath: ./fixtures/ts.security.predictable-token-generation/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []

