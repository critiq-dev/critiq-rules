apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/typescript/ts.security.frontend-only-authorization.rule.yaml
fixtures:
  - name: flags backend routes guarded only by frontend code
    observationPath: ./fixtures/ts.security.frontend-only-authorization/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ts.security.frontend-only-authorization
      allSeverities:
        - high
  - name: ignores observations without matching project facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
