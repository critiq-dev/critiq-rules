apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/typescript/ts.quality.no-barrel-file-cycle.rule.yaml
fixtures:
  - name: flags barrel cycle
    sourcePath: ./fixtures/ts.quality.no-barrel-file-cycle/invalid.ts
    expect:
      findingCount: 1
      allRuleIds:
        - ts.quality.no-barrel-file-cycle
  - name: allows non-cyclic barrel usage
    sourcePath: ./fixtures/ts.quality.no-barrel-file-cycle/valid.ts
    expect:
      findingCount: 0
      allRuleIds: []
