apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/typescript/ts.correctness.reassign-class-member.rule.yaml
fixtures:
  - name: flags reassign-class-member
    observationPath: ./fixtures/ts.correctness.reassign-class-member/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ts.correctness.reassign-class-member
      allSeverities:
        - medium
  - name: allows clean sample
    observationPath: ./fixtures/ts.correctness.reassign-class-member/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
