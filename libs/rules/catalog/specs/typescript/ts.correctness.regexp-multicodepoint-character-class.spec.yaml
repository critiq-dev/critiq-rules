apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/typescript/ts.correctness.regexp-multicodepoint-character-class.rule.yaml
fixtures:
  - name: flags regexp-multicodepoint-character-class
    observationPath: ./fixtures/ts.correctness.regexp-multicodepoint-character-class/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ts.correctness.regexp-multicodepoint-character-class
      allSeverities:
        - medium
  - name: allows clean sample
    observationPath: ./fixtures/ts.correctness.regexp-multicodepoint-character-class/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
