apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/typescript/ts.security.unsafe-marked-version.rule.yaml
fixtures:
  - name: flags outdated Markdown renderer dependency facts
    observationPath: ./fixtures/ts.security.unsafe-marked-version/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ts.security.unsafe-marked-version
      allSeverities:
        - high
  - name: ignores files without dependency policy facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
