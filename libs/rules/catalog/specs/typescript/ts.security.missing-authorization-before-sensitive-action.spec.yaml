apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/typescript/ts.security.missing-authorization-before-sensitive-action.rule.yaml
fixtures:
  - name: flags sensitive backend actions without authorization guards
    observationPath: ./fixtures/ts.security.missing-authorization-before-sensitive-action/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ts.security.missing-authorization-before-sensitive-action
      allSeverities:
        - high
  - name: ignores observations without matching project facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
