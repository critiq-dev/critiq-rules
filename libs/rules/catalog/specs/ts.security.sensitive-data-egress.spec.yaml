apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../rules/ts.security.sensitive-data-egress.rule.yaml
fixtures:
  - name: flags sensitive egress to external processor calls
    sourcePath: ./fixtures/ts.security.sensitive-data-egress/invalid.ts
    expect:
      findingCount: 3
      allRuleIds:
        - ts.security.sensitive-data-egress
        - ts.security.sensitive-data-egress
        - ts.security.sensitive-data-egress
      allSeverities:
        - high
        - high
        - high
  - name: ignores redacted and local-only payloads
    sourcePath: ./fixtures/ts.security.sensitive-data-egress/valid.ts
    expect:
      findingCount: 0
      allRuleIds: []
