apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../rules/ts.performance.repeated-io-in-loop.rule.yaml
fixtures:
  - name: flags IO repeated inside loops
    observationPath: ./fixtures/ts.performance.repeated-io-in-loop/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ts.performance.repeated-io-in-loop
      allSeverities:
        - high
  - name: ignores observations without matching project facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
