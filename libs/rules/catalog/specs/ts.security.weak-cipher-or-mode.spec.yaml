apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../rules/ts.security.weak-cipher-or-mode.rule.yaml
fixtures:
  - name: flags weak cipher usage
    observationPath: ./fixtures/ts.security.weak-cipher-or-mode/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ts.security.weak-cipher-or-mode
      allSeverities:
        - high
  - name: ignores approved cipher usage
    observationPath: ./fixtures/ts.security.weak-cipher-or-mode/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []

