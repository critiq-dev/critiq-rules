apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../rules/ts.security.insecure-password-hash-configuration.rule.yaml
fixtures:
  - name: flags legacy Argon2 password hash modes
    sourcePath: ./fixtures/public-security/auth-files.invalid.ts
    expect:
      findingCount: 1
      allRuleIds:
        - ts.security.insecure-password-hash-configuration
      allSeverities:
        - high
  - name: ignores argon2id password hashing
    sourcePath: ./fixtures/public-security/auth-files.valid.ts
    expect:
      findingCount: 0
      allRuleIds: []

