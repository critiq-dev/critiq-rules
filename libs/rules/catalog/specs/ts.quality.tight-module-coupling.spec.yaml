apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../rules/ts.quality.tight-module-coupling.rule.yaml
fixtures:
  - name: flags direct local import cycles
    observationPath: ./fixtures/ts.quality.tight-module-coupling/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ts.quality.tight-module-coupling
      allSeverities:
        - medium
  - name: ignores observations without matching project facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
