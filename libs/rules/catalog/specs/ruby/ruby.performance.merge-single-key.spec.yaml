apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/ruby/ruby.performance.merge-single-key.rule.yaml
fixtures:
  - name: flags single-key merge
    observationPath: ./fixtures/ruby.performance.merge-single-key/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ruby.performance.merge-single-key
      allSeverities:
        - medium
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
