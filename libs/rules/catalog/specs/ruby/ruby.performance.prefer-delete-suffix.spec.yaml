apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/ruby/ruby.performance.prefer-delete-suffix.rule.yaml
fixtures:
  - name: flags gsub with \z-anchored regex
    observationPath: ./fixtures/ruby.performance.prefer-delete-suffix/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ruby.performance.prefer-delete-suffix
      allSeverities:
        - medium
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
