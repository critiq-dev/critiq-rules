apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/ruby/ruby.performance.no-static-size-computation.rule.yaml
fixtures:
  - name: flags static array .count
    observationPath: ./fixtures/ruby.performance.no-static-size-computation/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ruby.performance.no-static-size-computation
      allSeverities:
        - medium
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
