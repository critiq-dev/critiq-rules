apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/ruby/ruby.performance.range-cover-over-include.rule.yaml
fixtures:
  - name: flags range include
    observationPath: ./fixtures/ruby.performance.range-cover-over-include/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - ruby.performance.range-cover-over-include
      allSeverities:
        - medium
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
