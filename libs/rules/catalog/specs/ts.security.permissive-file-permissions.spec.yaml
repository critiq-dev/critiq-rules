apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../rules/ts.security.permissive-file-permissions.rule.yaml
fixtures:
  - name: flags world-accessible file modes
    sourcePath: ./fixtures/public-security/auth-files.invalid.ts
    expect:
      findingCount: 1
      allRuleIds:
        - ts.security.permissive-file-permissions
      allSeverities:
        - high
  - name: ignores restrictive file modes
    sourcePath: ./fixtures/public-security/auth-files.valid.ts
    expect:
      findingCount: 0
      allRuleIds: []

