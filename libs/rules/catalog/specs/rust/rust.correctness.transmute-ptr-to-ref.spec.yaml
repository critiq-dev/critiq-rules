apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/rust/rust.correctness.transmute-ptr-to-ref.rule.yaml
fixtures:
  - name: flags transmute *const T to &T
    sourcePath: ./fixtures/rust.correctness.transmute-ptr-to-ref/invalid.rs
    expect:
      findingCount: 1
      allRuleIds:
        - rust.correctness.transmute-ptr-to-ref
  - name: accepts safe reference patterns
    sourcePath: ./fixtures/rust.correctness.transmute-ptr-to-ref/valid.rs
    expect:
      findingCount: 0
      allRuleIds: []
