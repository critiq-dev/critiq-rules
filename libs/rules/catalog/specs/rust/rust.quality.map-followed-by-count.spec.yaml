apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/rust/rust.quality.map-followed-by-count.rule.yaml
fixtures:
  - name: flags .map().count()
    sourcePath: ./fixtures/rust.quality.map-followed-by-count/invalid.rs
    expect:
      findingCount: 1
      allRuleIds:
        - rust.quality.map-followed-by-count
  - name: accepts .filter().count()
    sourcePath: ./fixtures/rust.quality.map-followed-by-count/valid.rs
    expect:
      findingCount: 0
      allRuleIds: []
