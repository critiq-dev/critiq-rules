apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/rust/rust.security.axum-insecure-cors-with-credentials.rule.yaml
fixtures:
  - name: flags very permissive CORS with credentials
    sourcePath: ./fixtures/rust.security.axum-insecure-cors-with-credentials/invalid.rs
    expect:
      findingCount: 1
      allRuleIds:
        - rust.security.axum-insecure-cors-with-credentials
  - name: accepts explicit origin with credentials
    sourcePath: ./fixtures/rust.security.axum-insecure-cors-with-credentials/valid.rs
    expect:
      findingCount: 0
      allRuleIds: []
