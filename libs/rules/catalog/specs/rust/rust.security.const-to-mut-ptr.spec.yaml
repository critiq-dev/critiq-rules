apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/rust/rust.security.const-to-mut-ptr.rule.yaml
fixtures:
  - name: flags const to mut pointer conversion
    sourcePath: ./fixtures/rust.security.const-to-mut-ptr/invalid.rs
    expect:
      findingCount: 1
      allRuleIds:
        - rust.security.const-to-mut-ptr
  - name: accepts safe pattern
    sourcePath: ./fixtures/rust.security.const-to-mut-ptr/valid.rs
    expect:
      findingCount: 0
      allRuleIds: []
