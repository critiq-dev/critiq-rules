apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/rust/rust.correctness.unit-argument.rule.yaml
fixtures:
  - name: flags unit value from extend used as argument
    sourcePath: ./fixtures/rust.correctness.unit-argument/invalid.rs
    expect:
      findingCount: 1
      allRuleIds:
        - rust.correctness.unit-argument
  - name: accepts decoupled mutation
    sourcePath: ./fixtures/rust.correctness.unit-argument/valid.rs
    expect:
      findingCount: 0
      allRuleIds: []
