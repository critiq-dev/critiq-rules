apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/rust/rust.correctness.unbounded-channel.rule.yaml
fixtures:
  - name: flags unbounded-channel
    sourcePath: ./fixtures/rust.correctness.unbounded-channel/invalid.rs
    expect:
      findingCount: 1
      allRuleIds:
        - rust.correctness.unbounded-channel
  - name: accepts safe alternative for unbounded-channel
    sourcePath: ./fixtures/rust.correctness.unbounded-channel/valid.rs
    expect:
      findingCount: 0
      allRuleIds: []
