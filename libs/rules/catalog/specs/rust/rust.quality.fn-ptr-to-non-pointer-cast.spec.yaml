apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/rust/rust.quality.fn-ptr-to-non-pointer-cast.rule.yaml
fixtures:
  - name: flags fn ptr to int cast
    sourcePath: ./fixtures/rust.quality.fn-ptr-to-non-pointer-cast/invalid.rs
    expect:
      findingCount: 1
      allRuleIds:
        - rust.quality.fn-ptr-to-non-pointer-cast
  - name: accepts fn ptr to ptr cast
    sourcePath: ./fixtures/rust.quality.fn-ptr-to-non-pointer-cast/valid.rs
    expect:
      findingCount: 0
      allRuleIds: []
