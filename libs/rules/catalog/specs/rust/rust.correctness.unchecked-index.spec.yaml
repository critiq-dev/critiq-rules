apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/rust/rust.correctness.unchecked-index.rule.yaml
fixtures:
  - name: flags unchecked-index
    sourcePath: ./fixtures/rust.correctness.unchecked-index/invalid.rs
    expect:
      findingCount: 1
      allRuleIds:
        - rust.correctness.unchecked-index
  - name: accepts safe alternative for unchecked-index
    sourcePath: ./fixtures/rust.correctness.unchecked-index/valid.rs
    expect:
      findingCount: 0
      allRuleIds: []
