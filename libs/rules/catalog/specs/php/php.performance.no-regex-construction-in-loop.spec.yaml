apiVersion: critiq.dev/v1alpha1
kind: RuleSpec
rulePath: ../../rules/php/php.performance.no-regex-construction-in-loop.rule.yaml
fixtures:
  - name: flags matching observation
    observationPath: ./fixtures/php.performance.no-regex-construction-in-loop/invalid.observation.json
    expect:
      findingCount: 1
      allRuleIds:
        - php.performance.no-regex-construction-in-loop
      allSeverities:
        - medium
  - name: ignores observations without matching facts
    observationPath: ./fixtures/project-common/valid.observation.json
    expect:
      findingCount: 0
      allRuleIds: []
