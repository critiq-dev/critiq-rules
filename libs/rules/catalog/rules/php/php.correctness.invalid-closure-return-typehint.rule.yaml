apiVersion: critiq.dev/v1alpha1
kind: Rule
metadata:
  id: php.correctness.invalid-closure-return-typehint
  version: v1
  title: Use valid closure return typehints
  summary: Use valid closure return typehints
  rationale: Use valid closure return typehints
  aliases:
    - PHP-T1001
  tags:
    - correctness
    - php
    - rules-catalog
  stability: stable
  appliesTo: block
scope:
  languages:
    - php
  paths:
    include:
      - "**/*.php"
match:
  fact:
    kind: php.correctness.invalid-closure-return-typehint
    bind: issue
emit:
  finding:
    category: correctness.language
    severity: critical
    confidence: 0.9
    tags:
      - correctness
      - php
  message:
    title: Use valid closure return typehints
    summary: "`${captures.issue.text}` matches php.correctness.invalid-closure-return-typehint."
  remediation:
    summary: Use valid closure return typehints
