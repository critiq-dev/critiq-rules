apiVersion: critiq.dev/v1alpha1
kind: Rule
metadata:
  id: cfn.maintainability.parameter-name-limit
  title: Parameter name limit
  summary: Parameter name limit
  rationale: cfn-lint reports I2011 when Parameter name limit.
  aliases:
    - CFLIN-I2011
  tags:
    - cfn
    - cfn-lint
    - cloudformation
    - i2011
    - rules-catalog
  stability: stable
  appliesTo: file
  detection:
    kind: pattern
scope:
  languages:
    - all
  paths:
    include:
      - "**/*.yaml"
      - "**/*.yml"
      - "**/*.json"
    exclude:
      - "**/node_modules/**"
match:
  fact:
    kind: cfn.lint.finding
    bind: finding
    where:
      - path: ruleId
        equals: I2011
emit:
  finding:
    category: maintainability.configuration
    severity: low
    confidence: 0.8
    tags:
      - cfn
      - cloudformation
  message:
    title: Parameter name limit
    summary: "${captures.finding.text} matches cfn-lint I2011."
  remediation:
    summary: Resolve the cfn-lint I2011 finding in this CloudFormation template.
