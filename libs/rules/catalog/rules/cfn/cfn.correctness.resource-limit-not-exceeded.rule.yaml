apiVersion: critiq.dev/v1alpha1
kind: Rule
metadata:
  id: cfn.correctness.resource-limit-not-exceeded
  version: v1
  title: Resource limit not exceeded
  summary: Resource limit not exceeded
  rationale: cfn-lint reports E3010 when Resource limit not exceeded.
  aliases:
    - CFLIN-E3010
  tags:
    - cfn
    - cfn-lint
    - cloudformation
    - e3010
    - rules-catalog
  stability: stable
  appliesTo: file
  detection:
    kind: pattern
scope:
  languages:
    - all
  paths:
    include:
      - "**/*.yaml"
      - "**/*.yml"
      - "**/*.json"
    exclude:
      - "**/node_modules/**"
match:
  fact:
    kind: cfn.lint.finding
    bind: finding
    where:
      - path: ruleId
        equals: E3010
emit:
  finding:
    category: correctness.configuration
    severity: high
    confidence: 0.9
    tags:
      - cfn
      - cloudformation
  message:
    title: Resource limit not exceeded
    summary: "${captures.finding.text} matches cfn-lint E3010."
  remediation:
    summary: Resolve the cfn-lint E3010 finding in this CloudFormation template.
