apiVersion: critiq.dev/v1alpha1
kind: Rule
metadata:
  id: cfn.correctness.select-validation-of-parameters
  title: Select validation of parameters
  summary: Select validation of parameters
  rationale: cfn-lint reports E1017 when Select validation of parameters.
  aliases:
    - CFLIN-E1017
  tags:
    - cfn
    - cfn-lint
    - cloudformation
    - e1017
    - rules-catalog
  stability: stable
  appliesTo: file
  detection:
    kind: pattern
scope:
  languages:
    - all
  paths:
    include:
      - "**/*.yaml"
      - "**/*.yml"
      - "**/*.json"
    exclude:
      - "**/node_modules/**"
match:
  fact:
    kind: cfn.lint.finding
    bind: finding
    where:
      - path: ruleId
        equals: E1017
emit:
  finding:
    category: correctness.configuration
    severity: high
    confidence: 0.9
    tags:
      - cfn
      - cloudformation
  message:
    title: Select validation of parameters
    summary: "${captures.finding.text} matches cfn-lint E1017."
  remediation:
    summary: Resolve the cfn-lint E1017 finding in this CloudFormation template.
