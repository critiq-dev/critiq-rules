apiVersion: critiq.dev/v1alpha1
kind: Rule
metadata:
  id: ts.correctness.duplicate-class-member
  version: v1
  title: Duplicate class member
  summary: Detects duplicate class member issues in JavaScript and TypeScript source.
  rationale: Duplicate class member patterns are a common source of bugs and confusing runtime behavior.
  aliases:
    - JS-0231
  tags:
    - correctness
    - language
    - rules-catalog
    - public-directory-parity
  stability: stable
  appliesTo: file
scope:
  languages:
    - typescript
    - javascript
match:
  fact:
    kind: language.duplicate-class-member
    bind: issue
emit:
  finding:
    category: correctness.language
    severity: medium
    confidence: 0.85
    tags:
      - correctness
      - language
  message:
    title: Fix duplicate class member
    summary: "Class member '${captures.issue.text}' is declared more than once. Duplicate members cause unexpected runtime behavior."
  remediation:
    summary: Remove the duplicate declaration or rename one of the members to have a distinct name.
