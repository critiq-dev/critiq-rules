apiVersion: critiq.dev/v1alpha1
kind: Rule
metadata:
  id: ts.performance.no-large-object-spread-in-loop
  version: v1
  title: Avoid object spread inside loops
  summary: Object spread inside loops creates repeated allocations and can degrade throughput in hot paths.
  rationale: Object spread inside loops creates repeated allocations and can degrade throughput in hot paths.
  tags:
    - performance
    - rules-catalog
  stability: stable
  appliesTo: block
scope:
  languages:
    - typescript
    - javascript
match:
  fact:
    kind: performance.no-large-object-spread-in-loop
    bind: issue
emit:
  finding:
    category: performance.allocation
    severity: low
    confidence: 0.8
    tags:
      - performance
  message:
    title: Avoid object spread inside loops
    summary: "`${captures.issue.text}` matches ts.performance.no-large-object-spread-in-loop."
  remediation:
    summary: Refactor this path to avoid repeated work in hot execution paths.
